# placepulse_models

Download the pretrained model files into `pretrained_weights/` before running the scoring script.

## Python Files

- `eval_two_years_MoreCity_pp.py`: loads the pretrained model and generates urban perception scores for the selected Street View images.

## Other Files

- `requirements.txt`: Python dependencies for running the perception scoring script.
